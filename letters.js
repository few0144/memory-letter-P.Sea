/* ================================================================
   💌 Memory Letter Box — จดหมายตั้งต้น (ใส่ไว้ในตู้ตั้งแต่แรก)
   ================================================================
   ตอนนี้ว่างเปล่า — จดหมายทั้งหมดมาจากที่คนเขียนผ่านหน้าเว็บ

   ถ้าอยากเพิ่มจดหมายตั้งต้น ใส่ก้อน { ... } ในวงเล็บ [] ได้เลย:

   const letters = [
     {
       sender: "แม่",                  // ชื่อผู้ส่ง
       title: "กำลังใจ",               // หัวข้อ
       message: "ภูมิใจในตัวลูกเสมอ",   // ข้อความ (ใช้ \n ขึ้นบรรทัดใหม่ได้)
       image: "images/mom.jpg"        // รูปภาพ — ไม่มีให้ใส่ ""
     },
   ];

   ⚠️ สูงสุด 15 ฉบับ
   ================================================================ */

const letters = [];
